import { QuestionComponent } from './question-component';

describe('QuestionComponent', () => {
  it('should create an instance', () => {
   //  expect(new QuestionComponent()).toBeTruthy();
  });
});
